// Sum Numbers
function sum (numbers) {
    let sum1=0;
    if(numbers.length<1) {
        return 0
    } else {
        for(let i=0; i<numbers.length; i++){
            sum1+=numbers[i];
        }
        return sum1;
    }
    "use strict";


};