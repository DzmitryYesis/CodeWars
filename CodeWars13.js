function arrayPlusArray(arr1, arr2) {
    var sumArray=arr1.concat(arr2);
    var sum = 0;
  for(var i = 0; i < sumArray.length; i++){
      sum += sumArray[i];
      }
    return sum; //something went wrong
  }